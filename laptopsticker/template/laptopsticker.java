import java.util.*;

public class laptopsticker {
    private static int solve(int wc, int hc, int ws, int hs) {
        return 0;
    }

    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int wc, hc, ws, hs;
        wc = in.nextInt();
        hc = in.nextInt();
        ws = in.nextInt();
        hs = in.nextInt();
        in.close();
        System.out.println(solve(wc, hc, ws, hs));
    }
}
